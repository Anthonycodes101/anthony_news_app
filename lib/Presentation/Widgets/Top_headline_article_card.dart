part of 'widgets.dart';

class TopHeadlineArticleCard extends StatelessWidget {
  const TopHeadlineArticleCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
