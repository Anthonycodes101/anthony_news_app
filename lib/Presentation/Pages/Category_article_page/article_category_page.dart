import 'package:flutter/material.dart';

class ArticleCeategoryPage extends StatelessWidget {
  const ArticleCeategoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
